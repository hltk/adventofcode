from utils import *
from aocd import data, submit

grid = data.split("\n")
n, m = len(grid), len(grid[0])
t = int(data)
t = ints(data)
for line in data.split("\n"):
    t = ints(line)

submit(r, part='a', day=XXX)
